class Solution(object):
    def mySqrt(self, x):
        if x ==0:
            return x
        r = x

        while r*r > x:
            # newtone method 
            r = (r + x//r)//2
        
        return r
# time nlogn and space n
